---
import AdminLayout from '@/layouts/AdminLayout.astro';
const user = Astro.locals.user;
if (!user) return Astro.redirect('/admin/login');
---
<AdminLayout title="Applications - test">
  <p>This is a test stub. If you see this in production, frontmatter is the issue.</p>
</AdminLayout>
