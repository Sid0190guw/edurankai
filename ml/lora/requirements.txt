# ml/lora — pinned, because a run you cannot reproduce cannot be compared to the next one.
#
# ml/intent has no manifest and its exact versions are no longer recoverable. That was survivable for
# one laptop run of a 66M encoder; it is not survivable for a program where run 2 has to be judged
# against run 1. Pin, and record the pin with the adapter.
#
# CUDA 12.1 wheels. For CPU-only smoke tests, install torch from the cpu index instead:
#   pip install torch==2.4.1 --index-url https://download.pytorch.org/whl/cpu
torch==2.4.1
transformers==4.44.2
peft==0.12.0
trl==0.9.6
datasets==2.21.0
accelerate==0.33.0
bitsandbytes==0.43.3     # 4-bit base for QLoRA; drop on CPU-only or Apple silicon
sentencepiece==0.2.0
safetensors==0.4.4
