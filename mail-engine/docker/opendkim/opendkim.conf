# mail-engine/docker/opendkim/opendkim.conf

Syslog                  yes
UMask                   007
Socket                  inet:8891@0.0.0.0
PidFile                 /run/opendkim/opendkim.pid
Background              no

# REQUIRED IN A CONTAINER, AND A DELIBERATE TRADE.
#
# OpenDKIM refuses by default to use a key file it considers unsafe — group- or world-readable, or
# not owned by the running user. The keys here arrive through a read-only bind mount from the host,
# so their ownership and mode are whatever the host filesystem says (on a Windows host, effectively
# 0777 owned by root), and NOTHING INSIDE THE CONTAINER CAN CHANGE THEM. With the check on, opendkim
# starts, logs "key data is not secure", and signs nothing — while the entrypoint has already printed
# "signing 1 domain(s)" because it only tested that the file exists.
#
# The check is protecting against other local users reading the key. In a single-purpose container
# whose only other process is opendkim itself, there are none; the real protection is the host's
# permissions on mail-engine/keys, which keys/README.md covers.
RequireSafeKeys         false

# sv = sign outgoing mail AND verify incoming. Verification is what puts a result into
# Authentication-Results, which is what a DMARC evaluation later reads.
Mode                    sv
Canonicalization        relaxed/simple
# relaxed/simple: relaxed header canonicalisation survives the whitespace changes MTAs make in
# transit; simple body canonicalisation is stricter but the body is not rewritten in practice.

KeyTable                /etc/opendkim/KeyTable
SigningTable            refile:/etc/opendkim/SigningTable
ExternalIgnoreList      refile:/etc/opendkim/TrustedHosts
InternalHosts           refile:/etc/opendkim/TrustedHosts

# The headers to sign. Matches DKIM_HEADER_FIELDS in src/dkim.ts, deliberately: a message must
# verify identically whether it was signed by OpenDKIM on the Postfix path or by the Node engine.
SignHeaders             from,to,cc,subject,date,message-id,mime-version,content-type,reply-to

# oversign From. Without this, an attacker can ADD a second From header to a signed message; the
# signature still verifies over the original, while mail clients display the added one.
OversignHeaders         From

SubDomains              no
AlwaysAddARHeader       yes
MinimumKeyBits          1024
