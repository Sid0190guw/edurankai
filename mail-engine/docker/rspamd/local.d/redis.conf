# Redis holds greylisting, rate-limit counters and the bayes corpus. Without it rspamd still runs,
# but it forgets everything it has learned on every restart — including which senders it has already
# greylisted, which makes greylisting a permanent tax rather than a one-time delay.
servers = "redis:6379";
