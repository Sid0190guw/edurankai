# Greylisting: answer 4xx the first time an unknown sender appears and accept the retry. It removes
# a large share of spam for nothing, at the cost of delaying the first message from a new
# correspondent — which is why the timeout is short.
timeout = 5min;
expire = 1d;
